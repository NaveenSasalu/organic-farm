name: Build and Deploy

on:
  push:
    branches: [main]
    paths:
      - "bend/**"
      - "fend/**"
      - ".github/workflows/build.yaml"

permissions:
  contents: write
  actions: read

env:
  REGISTRY: docker.io
  BACKEND_IMAGE: ${{ secrets.DOCKERHUB_USERNAME }}/farm-backend
  FRONTEND_IMAGE: ${{ secrets.DOCKERHUB_USERNAME }}/farm-frontend

jobs:
  # Determine what changed
  changes:
    runs-on: ubuntu-latest
    outputs:
      backend: ${{ steps.filter.outputs.backend }}
      frontend: ${{ steps.filter.outputs.frontend }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v2
        id: filter
        with:
          filters: |
            backend:
              - 'bend/**'
            frontend:
              - 'fend/**'

  # Build Backend
  build-backend:
    needs: changes
    if: needs.changes.outputs.backend == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Backend
        uses: docker/build-push-action@v6
        with:
          context: ./bend
          push: true
          tags: |
            ${{ env.BACKEND_IMAGE }}:${{ github.sha }}
            ${{ env.BACKEND_IMAGE }}:latest

  # Build Frontend
  build-frontend:
    needs: changes
    if: needs.changes.outputs.frontend == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Frontend
        uses: docker/build-push-action@v6
        with:
          context: ./fend
          push: true
          tags: |
            ${{ env.FRONTEND_IMAGE }}:${{ github.sha }}
            ${{ env.FRONTEND_IMAGE }}:latest

  # Update Kubernetes manifests (triggers ArgoCD)
  update-manifests:
    needs: [build-backend, build-frontend]
    if: always() && (needs.build-backend.result == 'success' || needs.build-frontend.result == 'success')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 0

      - name: Update Backend Image Tag
        if: needs.build-backend.result == 'success'
        run: |
          sed -i "s#farm-backend:.*#farm-backend:${{ github.sha }}#g" k8s/bend-deploy.yaml

      - name: Update Frontend Image Tag
        if: needs.build-frontend.result == 'success'
        run: |
          sed -i "s#farm-frontend:.*#farm-frontend:${{ github.sha }}#g" k8s/fend-deploy.yaml

      - name: Commit updated manifests
        uses: EndBug/add-and-commit@v9
        with:
          add: "k8s/"
          message: "GitOps: update image tags to ${{ github.sha }}"
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
